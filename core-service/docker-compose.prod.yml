services:
  core-service:
    image: ${ECR_REGISTRY}/pitch-coach/core-service:latest
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - pitchcoach-net

networks:
  pitchcoach-net:
    external: true
